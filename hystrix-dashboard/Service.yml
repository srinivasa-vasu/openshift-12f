apiVersion: v1
kind: Service
metadata:
  name: hystrixdb-service
  labels:
    app: hystrixdb-service
spec:
  ports:
    -
      name: hystrixdb-service
      port: 80
      targetPort: 8010
  selector:
    app: hystrixdb-service