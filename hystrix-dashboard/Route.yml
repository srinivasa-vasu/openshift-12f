apiVersion: v1
kind: Route
metadata:
  name: hystrixdb-service
  labels:
    app: hystrixdb-service
spec:
  to:
    kind: Service
    name: hystrixdb-service
    weight: 100
  port:
    targetPort: hystrixdb-service